# =============================================================
# MoviVIP — repo main LIMPIO
# Desde v7.0.0 el codigo fuente NO se sube a este repo.
# Vive en:  rama backup-v7-full-*  +  release ofuscada (install.sh)
# =============================================================

# --- Codigo fuente del paquete (protegido en releases) ---
bin/
build/
ddos/
functions/
gate/
herramientas/
languages/
launchers/
lib/
menu.sh
protocolos/
scripts/
servidor/
sistema/
tools/
usuarios/
install.sh
cambiar-licencia.sh
check-licencia.sh
descifrar-secrets.sh
validar-licencia.sh
DEPLOY-v5.2.md
banner.svg
.movivip-check.sh

# --- Python (cache de compilacion) ---
__pycache__/
*.pyc
*.pyo

# --- Sistema operativo ---
Thumbs.db
Desktop.ini

# --- Temporales ---
*.tmp
*.swp
scp_err.txt

# --- Backups y entregas locales ---
*.bak-*
*.bak
protocolos/entregas/

# =============================================================
# SEGURIDAD — NUNCA subir secretos/credenciales a git
# (tokens, IDs, payloads SNI, configs de operadoras, passwords)
# =============================================================
.env
.env.*
.env-bot
*.env
!*.env.example
.env.example

*.pem
*.key
*.crt
*.p12
*.jks
*service-account*.json
*service_account*.json
firebase*.json
credentials*.json

*operators*
*payloads*
*snis*

*.secret
*.token
id_rsa*
id_dsa*
*.ppk

config.py.bak*
admin_bot.py.bak*